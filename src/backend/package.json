{
  "name": "ikt2004-backend",
  "version": "1.0.0",
  "description": "Simple backend for IKT2004 exam project",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}

/* Tips: Run `npm install` here to install dependencies. Use `npm run dev` for auto-reload. */